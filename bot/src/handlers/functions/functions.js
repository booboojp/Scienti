const Discord = require('discord.js');
const fetch = reqire('node-fetch');

module.exports = async (client) => {
    client.loadSubcommands = async function (client, interaction, args) {
        try {

        } catch {
            return
        }
    }
}